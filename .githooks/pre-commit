#!/usr/bin/env bash
# Run local checks before creating a commit.

set -euo pipefail

if [ "${SKIP_HOOKS:-}" = "1" ]; then
  echo "Pre-commit hooks skipped (SKIP_HOOKS=1)"
  exit 0
fi

.githooks/run-local-checks
